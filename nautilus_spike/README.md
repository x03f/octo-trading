# Спайк NautilusTrader (изолированный venv)

Скрипт первого контакта. Venv в git НЕ хранится (тяжёлый, чужие зависимости):
воссоздать — `python3 -m venv /opt/octobot/nautilus-venv && /opt/octobot/nautilus-venv/bin/pip install nautilus_trader`.
Запуск: `/opt/octobot/nautilus-venv/bin/python spike_backtest.py`.
Результаты и вердикт — в `../NAUTILUS-SPIKE.md`.
